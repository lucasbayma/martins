---
phase: 01-architectural-split
plan: 05
type: execute
wave: 5
depends_on: [01-04]
files_modified:
  - src/app.rs
autonomous: false
requirements: [ARCH-01]

must_haves:
  truths:
    - "src/app.rs is ≤ 500 lines"
    - "src/app.rs contains only: App struct + SidebarItem + SelectionState + TabClick + impl App::new + impl App::run + App::refresh_diff + the small App field-accessor helpers (active_project/active_workspace/active_project_mut) + App::reattach_tmux_sessions + App::save_state + App::refresh_active_workspace_after_change + App::select_active_workspace + App::sync_pty_size + App::build_working_map + App::active_sessions + App::write_active_tab_input + App::copy_selection_to_clipboard + App::forward_key_to_pty + App::tab_at_column + App::open_new_tab_picker + the one-line delegators for draw/events/modal_controller/workspace"
    - "The app compiles, clippy-passes, fmt-passes, and runs with identical user-facing behavior"
    - "All four original tests in src/app.rs tests module still pass (app_new_without_git_repo, switch_project_updates_context, tab_click_detects_select_close_and_add) plus any test relocated to events.rs"
  artifacts:
    - path: "src/app.rs"
      provides: "Slim App state + event loop + construction"
      contains: "pub async fn run"
  key_links:
    - from: "src/app.rs::run"
      to: "src/events.rs::handle_event"
      via: "delegator inside tokio::select! arm"
      pattern: "events::handle_event"
    - from: "src/app.rs::run"
      to: "src/workspace.rs::create_workspace"
      via: "pending_workspace arm"
      pattern: "workspace::create_workspace|crate::workspace::create_workspace"
    - from: "src/app.rs::run"
      to: "src/ui/draw.rs::draw"
      via: "terminal.draw closure"
      pattern: "ui::draw::draw"
---

<objective>
After Plans 01–04 have extracted draw, modal controller, event routing, and workspace lifecycle, `src/app.rs` should already be under ~650 lines. This plan does the final pass: slim-down, audit what remains, collapse any now-redundant delegators, and verify the ROADMAP success criteria are satisfied.

Purpose: Hit the ROADMAP target of `src/app.rs ≤ ~500 lines` and produce a file where a reader can quickly understand the App struct, its construction, and the main event loop — without navigating through 2000 lines of tangential code.
Output: Final slim `src/app.rs` with clear structure, plus a PHASE-SUMMARY that confirms every ARCH-01 success criterion.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/codebase/CONVENTIONS.md

After prior plans, `src/app.rs` contains roughly:
- File-level imports
- SidebarItem enum (pub)
- SelectionState struct (pub) + impl
- App struct (pub, all fields pub)
- TabClick enum (pub(crate))
- impl App {
    - pub async fn new (construction, kicks off reattach_tmux_sessions + refresh_diff)
    - fn reattach_tmux_sessions (one-time boot session reattach — ~90 lines)
    - pub fn active_project / active_workspace / active_project_mut (small accessors)
    - pub async fn run (main event loop)
    - pub(crate) async fn refresh_diff
    - fn draw — one-line delegator to ui::draw::draw
    - async fn handle_event / handle_key / handle_mouse / handle_scroll / handle_click / handle_picker_click / handle_modal_key / handle_modal_click / apply_picker_outcome / dispatch_action / activate_sidebar_item — one-line delegators
    - pub(crate) all lifecycle methods — one-line delegators (switch_project, archive_active_workspace, delete_archived_workspace, confirm_delete_workspace, confirm_remove_project, queue_workspace_creation, create_workspace, create_tab, add_project_from_path)
    - pub(crate) fn save_state
    - pub(crate) fn refresh_active_workspace_after_change
    - pub(crate) fn select_active_workspace
    - pub(crate) fn open_new_tab_picker
    - pub(crate) fn write_active_tab_input
    - pub(crate) fn copy_selection_to_clipboard
    - pub(crate) fn forward_key_to_pty
    - fn sync_pty_size
    - fn build_working_map
    - pub(crate) fn active_sessions
    - pub(crate) fn tab_at_column
  }
- #[cfg(test)] mod tests — 3 tests

Total after Plan 04 should be approximately 570–650 lines. The main bulk left is:
- `reattach_tmux_sessions` (~90 lines)
- `run` (~50 lines)
- `new` (~55 lines)
- `dispatch_action`, `handle_event`, etc. delegators (~30 lines total — 9 delegators × ~3 lines)
- `sync_pty_size` (~35 lines)
- the small helpers (~20 lines each for write_active_tab_input, forward_key_to_pty, tab_at_column, build_working_map, copy_selection_to_clipboard, active_sessions, refresh_diff)
- tests (~90 lines)

To get under 500 lines, the plan is to collapse the delegator boilerplate that no longer belongs on App at all. After Plan 03 moved event routing out and Plan 04 moved lifecycle out, the `impl App` delegators are pure call-forwarders. If they can be removed entirely — with their sole call sites (App::run, and test-code if any) updated to call through `crate::events::` / `crate::workspace::` / `crate::ui::modal_controller::` / `crate::ui::draw::` directly — most of the delegator fat disappears.

@src/app.rs
@src/events.rs
@src/workspace.rs
@src/ui/draw.rs
@src/ui/modal_controller.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit current src/app.rs and remove delegator methods that have no internal callers</name>
  <read_first>
    - src/app.rs (complete file — inventory every method)
    - src/events.rs (check which App delegators it calls — for each, we want direct `crate::X::fn_name(app, ...)` instead of `app.fn_name(...)`)
    - src/workspace.rs (same check)
    - src/ui/modal_controller.rs (same check)
    - src/ui/draw.rs (same check)
  </read_first>
  <files>src/app.rs</files>
  <action>
    Decide, method-by-method, whether each delegator on `impl App` has any remaining internal caller. Survey by searching the entire codebase for `app.fn_name(` and `self.fn_name(` references.

    **Process: for each delegator method on App (draw, handle_event, handle_key, handle_mouse, handle_scroll, handle_click, handle_picker_click, handle_modal_key, handle_modal_click, apply_picker_outcome, dispatch_action, activate_sidebar_item, switch_project, archive_active_workspace, delete_archived_workspace, confirm_delete_workspace, confirm_remove_project, queue_workspace_creation, create_workspace, create_tab, add_project_from_path):**

    1. Grep the codebase for `self.{method}(`  and `app.{method}(`  and `.{method}(`  in all source files.
    2. If the ONLY caller is the delegator itself (self-reference) or an external module that should call the free function directly, **remove the delegator** and update the caller.
    3. If an internal method in app.rs (e.g., App::run calls self.refresh_diff, self.draw, self.handle_event, self.sync_pty_size), the delegator must stay OR App::run's call must be redirected.

    **Specific decisions:**

    **KEEP these as App methods (they have legitimate App-internal callers or are the App's own public API):**
    - `async fn new` — keep (it's the constructor, called from main.rs)
    - `async fn run` — keep (it's the main entry, called from main.rs)
    - `async fn refresh_diff` — keep; called from App::run's tokio::select! and App::new. Redirecting these to `crate::events::refresh_diff` would be weird; refresh_diff is pure App state — leave in app.rs. Remove `pub(crate)` if events.rs and workspace.rs don't call it — re-check. They DO call it. Keep `pub(crate)`.
    - `async fn handle_event` — keep as-is (one-line delegator). Called from App::run's tokio::select! Can't easily avoid the delegator without calling `crate::events::handle_event(self, event).await` directly inside `run()`. **OPTION: inline the call in App::run**. Decision: **inline into run()**. Remove the delegator.
    - `fn draw` — keep delegator OR inline into `run()`'s `terminal.draw(|frame| self.draw(frame))` → `terminal.draw(|frame| crate::ui::draw::draw(self, frame))`. **Decision: inline. Remove the delegator.**
    - `fn sync_pty_size` — keep (internal App method, no extraction)
    - `fn reattach_tmux_sessions` — keep (internal App method called by App::new)
    - `active_project` / `active_workspace` / `active_project_mut` — keep (accessors used by every extracted module)
    - `save_state`, `refresh_active_workspace_after_change`, `select_active_workspace` — keep (small App-internal state mutations)
    - `build_working_map`, `active_sessions` — keep (used by draw.rs and events.rs; App-internal helpers)
    - `write_active_tab_input`, `copy_selection_to_clipboard`, `forward_key_to_pty` — keep (called by events.rs; touch App's PTY manager)
    - `tab_at_column` — keep (called by events.rs)
    - `open_new_tab_picker` — keep (called by events.rs and workspace.rs)

    **REMOVE these delegators (inline the call at the only internal call site, which is App::run; external callers use the free function directly):**
    - `async fn handle_event` — only caller is App::run. **Action: remove the method, change `self.handle_event(event).await` in App::run to `crate::events::handle_event(self, event).await`.**
    - `fn draw` — only caller is App::run's closure. **Action: remove the method, change `terminal.draw(|frame| self.draw(frame))?` to `terminal.draw(|frame| crate::ui::draw::draw(self, frame))?`.**
    - `async fn handle_key` — NO internal callers after removing handle_event. events::handle_event now calls `handle_key(app, key)` as a free function. **Action: remove the App method.**
    - `async fn handle_mouse` — same. **Remove.**
    - `fn handle_scroll` — same. **Remove.**
    - `async fn handle_click` — same. **Remove.**
    - `async fn handle_picker_click` — same. **Remove.**
    - `async fn handle_modal_key` — same. **Remove.**
    - `async fn handle_modal_click` — same. **Remove.**
    - `async fn apply_picker_outcome` — same. **Remove.**
    - `async fn dispatch_action` — same. **Remove.**
    - `async fn activate_sidebar_item` — same. **Remove.**

    **KEEP these lifecycle delegators — they are called INTERNALLY from App::run:**
    - `async fn create_workspace` — called from App::run's `pending_workspace` arm. **Change call site in App::run: `match self.create_workspace(name).await` → `match crate::workspace::create_workspace(self, name).await`. Then remove the delegator on App.**

    All other lifecycle methods (switch_project, archive_*, delete_*, confirm_*, queue_workspace_creation, create_tab, add_project_from_path) have NO internal App callers after Plans 03-04 — they were only called from events::dispatch_action (which takes `&mut App` and now calls `crate::workspace::fn(app, ...)` directly). **Remove all their delegators.**

    **Before removal, verify each with:**
    ```bash
    grep -n "self\\.{method}\\|\\.\\_{method}\\|app\\.{method}" src/*.rs src/ui/*.rs src/pty/*.rs src/git/*.rs
    ```

    If a remaining match is found OTHER than the delegator definition itself, do NOT remove — investigate. Update the caller to use the free-function form, THEN remove.

    **Outcome:** `impl App` drops ~60–70 lines of delegator boilerplate. All call sites that used to go through App now call the free functions directly, which is also semantically cleaner (the App struct no longer pretends to have an "API" for routing — the modules do).

    **Update tests:** If the test `tab_click_detects_select_close_and_add` in src/app.rs calls `app.tab_at_column(...)`, that stays (tab_at_column remains an App method). No test change needed.
  </action>
  <verify>
    <automated>cargo check --manifest-path Cargo.toml 2>&1 | tee /tmp/check-01-05-task1.log; grep -q "error\[" /tmp/check-01-05-task1.log && exit 1; cargo clippy --all-targets -- -D warnings 2>&1 | tee /tmp/clippy-01-05-task1.log; grep -q "error" /tmp/clippy-01-05-task1.log && exit 1; cargo test --lib 2>&1 | tee /tmp/test-01-05-task1.log; grep -q "FAILED" /tmp/test-01-05-task1.log && exit 1 || exit 0</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "async fn handle_event" src/app.rs` returns `0`
    - `grep -c "fn draw(&mut self" src/app.rs` returns `0`
    - `grep -c "async fn handle_key" src/app.rs` returns `0`
    - `grep -c "async fn handle_mouse" src/app.rs` returns `0`
    - `grep -c "fn handle_scroll" src/app.rs` returns `0`
    - `grep -c "async fn dispatch_action" src/app.rs` returns `0`
    - `grep -c "async fn handle_modal_key" src/app.rs` returns `0`
    - `grep -c "async fn handle_modal_click" src/app.rs` returns `0`
    - `grep -c "async fn switch_project" src/app.rs` returns `0`
    - `grep -c "fn archive_active_workspace" src/app.rs` returns `0`
    - `grep -c "fn delete_archived_workspace" src/app.rs` returns `0`
    - `grep -c "fn confirm_delete_workspace" src/app.rs` returns `0`
    - `grep -c "async fn confirm_remove_project" src/app.rs` returns `0`
    - `grep -c "fn queue_workspace_creation" src/app.rs` returns `0`
    - `grep -c "async fn create_workspace" src/app.rs` returns `0`
    - `grep -c "async fn create_tab" src/app.rs` returns `0`
    - `grep -c "async fn add_project_from_path" src/app.rs` returns `0`
    - `grep -q "crate::events::handle_event(self, event)" src/app.rs` succeeds (inlined in App::run)
    - `grep -q "crate::ui::draw::draw(self, frame)" src/app.rs` succeeds (inlined in App::run's terminal.draw closure)
    - `grep -q "crate::workspace::create_workspace(self, name)" src/app.rs` succeeds (inlined in App::run's pending_workspace arm)
    - `cargo check` exits 0
    - `cargo clippy --all-targets -- -D warnings` exits 0
    - `cargo fmt --check` exits 0
    - `cargo test --lib` exits 0
  </acceptance_criteria>
  <done>Delegators removed. App::run calls free functions directly at every site. All gates pass.</done>
</task>

<task type="auto">
  <name>Task 2: Final line-count audit and structural review</name>
  <read_first>
    - src/app.rs (re-read the entire file in its final state)
  </read_first>
  <files>src/app.rs</files>
  <action>
    With delegators removed, measure and verify:

    1. Run `wc -l src/app.rs`. Must report ≤ 500.
    2. If the count is between 500 and 550, do one or more of:
       - Move `#[cfg(test)] mod tests` to a sibling file `src/app_tests.rs` (declared with `#[cfg(test)] mod app_tests;` — unusual pattern; only do this if needed for the line budget).
       - Review `reattach_tmux_sessions` — at ~90 lines it is a candidate for extraction. If after delegator removal the file is still over 500 lines, extract `reattach_tmux_sessions` to `src/workspace.rs` as `pub(crate) fn reattach_tmux_sessions(app: &mut App)`. Update App::new to call `crate::workspace::reattach_tmux_sessions(&mut self_ref)` — but `self` in App::new hasn't been constructed yet at the point of call. Adjust: call it AFTER `let mut app = Self { ... };` — which is exactly where it is today. So inside App::new, replace `app.reattach_tmux_sessions()` with `crate::workspace::reattach_tmux_sessions(&mut app)`, then delete the impl.
    3. If the count is ≤ 500 already, skip the extraction.

    **Imports audit:**
    After this final pass, `use` block at top of src/app.rs should be minimal. Likely-remaining imports:
    ```rust
    use crate::git::diff;
    use crate::keys::{InputMode, Keymap};
    use crate::pty::manager::PtyManager;
    use crate::state::{GlobalState, Project, Workspace};
    use crate::ui::layout::{LayoutState, PaneRects};
    use crate::ui::modal::Modal;
    use crate::ui::picker::Picker;
    use anyhow::Result;
    use crossterm::event::{EventStream, KeyEvent};
    use futures::StreamExt;
    use ratatui::{DefaultTerminal, layout::Rect, widgets::ListState};
    use std::path::PathBuf;
    use std::time::Duration;
    use tokio::time::interval;
    ```

    Run `cargo clippy --all-targets -- -D warnings` and let it surface any unused import. Remove what it flags.

    **Final structure check** — `src/app.rs` should read top-to-bottom as:
    1. Module doc comment
    2. Imports
    3. `SidebarItem` (pub enum)
    4. `SelectionState` (pub struct + impl)
    5. `App` (pub struct)
    6. `TabClick` (pub(crate) enum) — used by events.rs and the test
    7. `impl App` with only:
       - `pub async fn new`
       - (possibly) `fn reattach_tmux_sessions` if not extracted
       - `pub fn active_project`, `pub(crate) fn active_project_mut`, `pub(crate) fn active_workspace`
       - `pub async fn run`
       - `pub(crate) async fn refresh_diff`
       - `pub(crate) fn save_state`
       - `pub(crate) fn refresh_active_workspace_after_change`
       - `pub(crate) fn select_active_workspace`
       - `pub(crate) fn open_new_tab_picker`
       - `pub(crate) fn write_active_tab_input`
       - `pub(crate) fn copy_selection_to_clipboard`
       - `pub(crate) fn forward_key_to_pty`
       - `fn sync_pty_size`
       - `fn build_working_map`
       - `pub(crate) fn active_sessions`
       - `pub(crate) fn tab_at_column`
    8. `#[cfg(test)] mod tests` (3 tests)

    Anything else still in the file is a candidate for further extraction. Flag it in the output SUMMARY.
  </action>
  <verify>
    <automated>WC=$(wc -l < src/app.rs); echo "lines=$WC"; test "$WC" -le 500 || exit 1; cargo check --manifest-path Cargo.toml 2>&1 | grep -q "error\[" && exit 1; cargo clippy --all-targets -- -D warnings 2>&1 | grep -q "error" && exit 1; cargo fmt --check 2>&1 | grep -q "Diff" && exit 1; cargo test --lib 2>&1 | grep -q "FAILED" && exit 1 || exit 0</automated>
  </verify>
  <acceptance_criteria>
    - `wc -l src/app.rs` reports a line count ≤ 500
    - `grep -q "pub async fn run" src/app.rs` succeeds (the main loop is still here)
    - `grep -q "pub struct App" src/app.rs` succeeds (struct definition is still here)
    - `grep -q "pub async fn new" src/app.rs` succeeds (constructor is still here)
    - `cargo check` exits 0
    - `cargo clippy --all-targets -- -D warnings` exits 0
    - `cargo fmt --check` exits 0
    - `cargo test --lib` exits 0
  </acceptance_criteria>
  <done>src/app.rs ≤ 500 lines. Only App state + event loop + construction remain. All gates green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Full end-to-end validation against ROADMAP success criteria</name>
  <files>N/A — manual verification only</files>
  <action>Human runs all static + runtime checks listed in <how-to-verify> and confirms every ROADMAP criterion is satisfied.</action>
  <verify>Manual verification via the how-to-verify steps below.</verify>
  <done>User types 'approved' per <resume-signal>.</done>
  <what-built>All extractions complete: src/app.rs ≤ 500 lines, events.rs houses event routing, ui/modal_controller.rs houses modal dispatch, workspace.rs houses lifecycle, ui/draw.rs houses draw orchestration.</what-built>
  <how-to-verify>
    **Static verification (do these first, they are fast):**
    1. `wc -l src/app.rs` — report the number. Must be ≤ 500.
    2. `wc -l src/events.rs src/workspace.rs src/ui/modal_controller.rs src/ui/draw.rs` — report each. All ≤ 500. (If any one exceeds, this phase has not met the single-responsibility cap.)
    3. `cargo check` — exit 0
    4. `cargo clippy --all-targets -- -D warnings` — exit 0
    5. `cargo fmt --check` — exit 0
    6. `cargo test` — exit 0 (full test suite including integration tests)

    **Runtime validation (do a full once-over of the app):**
    7. `cargo run --release` in a repo with existing workspaces → tmux sessions reattach, sidebar shows all projects + workspaces, active workspace's PTY content renders immediately
    8. Type in the PTY — characters appear (no regression vs pre-refactor)
    9. Ctrl-B → NORMAL mode → arrow keys navigate sidebar → select a different workspace → its PTY content appears in main pane
    10. Enter on an empty workspace → new-tab picker opens
    11. `n` → new workspace → type a name → Enter → workspace creates, tab starts, tmux session spawns
    12. `t` → pick "shell" → new tab appears, F2 switches to it
    13. Click on a file in right sidebar (git diff list) → opens a `diff <path>` tab showing git diff via less
    14. Click on ✕ on a tab → tab closes, tmux session killed
    15. Archive a workspace via right-edge sidebar click → tmux sessions killed, worktree dir removed
    16. `q` → ConfirmQuit → Enter → app exits, state.json written

    **If ANY of these fails, the phase is incomplete.** Report exactly which check fails.
  </how-to-verify>
  <resume-signal>Type "approved" only if all 16 checks pass. Otherwise list the failing check numbers with observed vs expected behavior.</resume-signal>
</task>

<task type="auto">
  <name>Task 4: Write PHASE SUMMARY mapping outputs to ROADMAP success criteria</name>
  <read_first>
    - .planning/ROADMAP.md (Phase 1 success criteria)
    - .planning/phases/01-architectural-split/01-01-SUMMARY.md
    - .planning/phases/01-architectural-split/01-02-SUMMARY.md
    - .planning/phases/01-architectural-split/01-03-SUMMARY.md
    - .planning/phases/01-architectural-split/01-04-SUMMARY.md
  </read_first>
  <files>.planning/phases/01-architectural-split/PHASE-SUMMARY.md</files>
  <action>
    Write `.planning/phases/01-architectural-split/PHASE-SUMMARY.md` with this structure:

    ```markdown
    # Phase 1: Architectural Split — Summary

    **Completed:** YYYY-MM-DD (today's date)
    **Requirement:** ARCH-01

    ## ROADMAP Success Criteria Check

    1. **`src/app.rs` ≤ ~500 lines, contains only App struct + run() loop.**
       - Status: [PASS/FAIL]
       - Actual line count: N
       - File contents: App struct, SidebarItem, SelectionState, TabClick, impl App (constructor, run loop, small accessors, PTY helpers, state helpers), tests module

    2. **Event routing in its own module.**
       - Status: [PASS/FAIL]
       - Location: `src/events.rs`
       - Line count: N
       - Functions: handle_event, handle_key, handle_mouse, handle_scroll, handle_click, dispatch_action, activate_sidebar_item, handle_picker_click, apply_picker_outcome + helpers (rect_contains, terminal_content_rect, menu_action_at_column, key_to_bytes, move_*, picker_area)

    3. **Modal dispatch in its own module.**
       - Status: [PASS/FAIL]
       - Location: `src/ui/modal_controller.rs`
       - Line count: N
       - Functions: handle_modal_key, handle_modal_click, modal_button_row_y, is_modal_first_button

    4. **Workspace lifecycle in its own module.**
       - Status: [PASS/FAIL]
       - Location: `src/workspace.rs`
       - Line count: N
       - Functions: create_workspace, create_tab, archive_active_workspace, delete_archived_workspace, confirm_delete_workspace, confirm_remove_project, switch_project, add_project_from_path, queue_workspace_creation, tab_program_for_new, tab_program_for_resume

    5. **Compiles, runs, identical behavior.**
       - Status: [PASS/FAIL]
       - Verified by plan-level smoke tests (01-01 Task 3, 01-02 Task 3, 01-03 Task 4, 01-04 Task 3, 01-05 Task 3)
       - `cargo check`, `cargo clippy --all-targets -- -D warnings`, `cargo fmt --check`, `cargo test` all pass

    ## Module Line Counts (post-phase)

    | File | Lines |
    |------|-------|
    | src/app.rs | N |
    | src/events.rs | N |
    | src/workspace.rs | N |
    | src/ui/draw.rs | N |
    | src/ui/modal_controller.rs | N |

    ## App Method Visibility Changes

    Methods promoted to `pub(crate)` during extractions:
    - List each with the plan that promoted it.

    ## Patterns Established for Future Phases

    - **Free-function + &mut App extraction pattern**: every extracted module exposes `pub async fn foo(app: &mut App, ...)` instead of restructuring App into sub-structs. This keeps borrow-checker friction low because each function still only takes one `&mut App` at a time.
    - **Sibling module for non-UI coordination**: events.rs and workspace.rs are top-level modules, not under `src/ui/`. UI-only coordination (draw, modal dispatch) goes under `src/ui/`.
    - **Preserve call ordering in lifecycle code**: state save points and subprocess invocations must match the original sequence. Audited per plan.

    ## Known Follow-Ups

    - Phase 2 (Event Loop Rewire) will edit `src/app.rs::run` to introduce the dirty-flag and add a dedicated input-priority branch. The slim `run()` makes this safe to do.
    - CONCERNS.md items #2 (`.unwrap()` audit), #4 (`#![allow(dead_code)]` in state.rs) remain deferred.
    - Modal module split (CONCERNS.md #3) not done in this phase.
    ```

    Fill in actual line counts and PASS/FAIL verdicts from the verified state of the repo.
  </action>
  <verify>
    <automated>test -f .planning/phases/01-architectural-split/PHASE-SUMMARY.md && grep -q "ROADMAP Success Criteria Check" .planning/phases/01-architectural-split/PHASE-SUMMARY.md && grep -q "Module Line Counts" .planning/phases/01-architectural-split/PHASE-SUMMARY.md || exit 1</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/01-architectural-split/PHASE-SUMMARY.md` exists
    - It contains sections "ROADMAP Success Criteria Check", "Module Line Counts", "Patterns Established for Future Phases"
    - Every of the 5 ROADMAP success criteria has a PASS/FAIL verdict backed by a line count or a test command
  </acceptance_criteria>
  <done>PHASE-SUMMARY.md committed. Phase 1 complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries in this plan. Pure mechanical cleanup of delegators + audit.

| Boundary | Description |
|----------|-------------|
| (none) | Refactor-only, no new surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05-01 | Tampering (call-site redirection introduces typo) | App::run inlined calls | mitigate | Verified by `cargo check` (compiler catches any unresolved path). Task 3 runtime smoke test exercises the full path. |
| T-01-05-02 | Denial of Service (accidentally keep a delegator that no longer has a body but still has a signature in test code) | tests module in app.rs | mitigate | Acceptance criteria grep checks that each delegator is absent; `cargo test` catches test breakage; manual test check in Task 3 catches runtime breakage. |

No high-severity threats. This plan is the safest in the phase — it only removes code that was already dead-weight delegators.
</threat_model>

<verification>
- `wc -l src/app.rs` ≤ 500
- `cargo check`, `cargo clippy --all-targets -- -D warnings`, `cargo fmt --check`, `cargo test` all exit 0
- Task 3 runtime smoke test passes all 16 checks
- Task 4 PHASE-SUMMARY.md exists with PASS verdicts for every ROADMAP criterion
</verification>

<success_criteria>
Every ROADMAP Phase 1 success criterion is satisfied and documented in PHASE-SUMMARY.md. The app runs with identical observable behavior to pre-refactor.
</success_criteria>

<output>
After completion, create `.planning/phases/01-architectural-split/01-05-SUMMARY.md` with:
- Final line counts for all touched files
- List of delegators removed
- Whether reattach_tmux_sessions was extracted (and to where) or kept in app.rs
- Confirmation that PHASE-SUMMARY.md was written
- Any surprise surfaced during the final pass
</output>
