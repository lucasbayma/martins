---
phase: 07-tmux-native-main-screen-selection
plan: 06
type: execute
wave: 4
depends_on: [01, 02, 03, 04, 05]
files_modified:
  - .planning/phases/07-tmux-native-main-screen-selection/07-HUMAN-UAT.md
autonomous: false
requirements: [SEL-01, SEL-02, SEL-03, SEL-04]
tags: [phase-7, uat, manual-verification, dual-path, ghostty-parity]

must_haves:
  truths:
    - "PTY-pane drag-select in a non-mouse-app session (bash, zsh) feels indistinguishable from running tmux directly in Ghostty"
    - "PTY-pane drag-select in a mouse-app session (vim mouse=a, htop, btop) still uses Phase 6 REVERSED-XOR overlay (SEL-01..04 still hold)"
    - "Mouse-up in delegate path auto-copies selection to macOS clipboard (verifiable via pbpaste in another terminal)"
    - "cmd+c precedence Tier 1 (overlay) → Tier 2 (tmux buffer) → Tier 3 (SIGINT) all fire correctly across both paths"
    - "Single Esc exits tmux copy-mode (D-14 vi-mode override holds)"
    - "Tab/workspace switch cancels outgoing tmux selection (D-16)"
    - "Overlay→tmux transition (vim quit then drag) leaves no stale highlight (Pitfall #2 mitigation works)"
  artifacts:
    - path: ".planning/phases/07-tmux-native-main-screen-selection/07-HUMAN-UAT.md"
      provides: "Operator-signed UAT log with PASS/FAIL for each of UAT-7-A..K + forward-looking notes for Phase 8 if any"
      contains: "UAT-7-K"
  key_links:
    - from: "07-HUMAN-UAT.md"
      to: "07-RESEARCH.md §Validation Architecture > Manual UAT"
      via: "test case ID mapping"
      pattern: "UAT-7-A"
---

<objective>
Operator-driven dual-path UAT verifying Phase 7's headline goal: PTY-pane selection in non-mouse-app sessions feels indistinguishable from running tmux directly in Ghostty, while mouse-app sessions retain the Phase 6 overlay path. Eleven structured test cases (UAT-7-A..K) cross-comparing Martins against a side-by-side Ghostty+tmux baseline.

Purpose: Per 07-RESEARCH.md §Validation Architecture lines 965-989 the qualitative target ("feels native") cannot be automated. The operator runs each scripted scenario in both Martins and Ghostty-direct-tmux, then signs off PASS/FAIL with brief notes. UAT also catches the items the unit suite explicitly defers: Tier-2 cmd+c subprocess-end-to-end, Esc-byte-forwarding-into-tmux-cancel, scroll-during-tmux-selection (Pitfall #6), Esc-asymmetry override correctness (Pitfall #5).

Output: 07-HUMAN-UAT.md with operator sign-off; subsequent merge to PHASE-SUMMARY.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-tmux-native-main-screen-selection/07-CONTEXT.md
@.planning/phases/07-tmux-native-main-screen-selection/07-RESEARCH.md
@.planning/phases/07-tmux-native-main-screen-selection/07-VALIDATION.md
@.planning/phases/07-tmux-native-main-screen-selection/07-04-SUMMARY.md
@.planning/phases/07-tmux-native-main-screen-selection/07-05-SUMMARY.md
@.planning/phases/06-text-selection/06-HUMAN-UAT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build release binary + create UAT scaffolding file</name>
  <read_first>
    - .planning/phases/06-text-selection/06-HUMAN-UAT.md (Phase 6 UAT format precedent — header, table layout, sign-off block, forward-looking notes section)
    - .planning/phases/07-tmux-native-main-screen-selection/07-RESEARCH.md §Validation Architecture > Manual UAT lines 965-989 (UAT-7-A..K full text)
  </read_first>
  <action>
**Step 1.** Compile the release binary so the operator UAT runs against optimized code (matches the perceived-latency guarantee from RESEARCH §Subprocess Behavior — debug builds add ~2x overhead per Phase 5 lessons):

```bash
cargo build --release
```

Confirm the binary at `target/release/martins` exists.

**Step 2.** Create `.planning/phases/07-tmux-native-main-screen-selection/07-HUMAN-UAT.md` with the following exact scaffolding. Operator fills in Result + Notes columns + sign-off section during the checkpoint task below:

```markdown
# Phase 7 — Human UAT

**Phase:** 07-tmux-native-main-screen-selection
**Date:** TBD (operator-filled)
**Operator:** lucasobayma@gmail.com

## Setup

- Ghostty terminal A (top half of screen): run `tmux new -s baseline` directly. This is the reference baseline for "what tmux selection feels like natively in Ghostty".
- Ghostty terminal B (bottom half): run `target/release/martins` (Phase 7 build). This is the unit under test.
- Open both side-by-side at roughly equal pane sizes so feel comparison is direct.
- Have a third terminal pane available with `pbpaste` ready for clipboard verification.

## Cross-Path UAT Cases

> Maps to 07-RESEARCH.md §Validation Architecture > Manual UAT.

| ID | Path | Procedure | Expected | Result | Notes |
|----|------|-----------|----------|--------|-------|
| UAT-7-A | tmux native (delegate) | In Martins active tab running `bash` (no mouse-app), drag-select a line | Highlight in tmux's own reverse-video; `pbpaste` returns selected text after release. **Feel matches Ghostty A side-by-side.** |  |  |
| UAT-7-B | tmux native (delegate) | Double-click a word in `bash` | Word highlights and lands on clipboard immediately |  |  |
| UAT-7-C | tmux native (delegate) | Triple-click a line | Line highlights and lands on clipboard |  |  |
| UAT-7-D | tmux native (delegate) | Drag-select then press `Esc` | Selection clears + copy-mode exits in **single press** (verifies Plan 07-02 Esc override) |  |  |
| UAT-7-E | tmux native (delegate) | Drag-select then click outside the selection | Selection clears (tmux's own behavior + Plan 07-04 forward Down(Left) re-enters select) |  |  |
| UAT-7-F | tmux native (delegate) | Drag-select then press `cmd+c` | `pbpaste` returns selected text (Plan 07-05 Tier 2 path) |  |  |
| UAT-7-G | overlay (mouse-app: vim) | Run `vim`, `:set mouse=a`, drag-select | Phase 6 overlay highlight (REVERSED) appears, NOT tmux's. SEL-01..04 still hold. |  |  |
| UAT-7-H | overlay (mouse-app: htop) | Run `htop`, drag-select | Same as UAT-7-G — overlay path active because htop sets DECSET 1003 |  |  |
| UAT-7-I | overlay → tmux transition | In tab: run `vim`, drag-select (overlay), Esc, `:q`, then drag-select again | After `:q`, vim resets mouse mode; next drag uses tmux native. **No stale REVERSED highlight from vim session** (Pitfall #2 mitigation in Plan 07-04) |  |  |
| UAT-7-J | tab switch with active tmux selection | Tab 1 (delegate path): drag-select, leave selected. F-key to tab 2. | Tab 1's tmux selection canceled (verify by switching back: no highlight) — Plan 07-03 set_active_tab D-16 |  |  |
| UAT-7-K | cmd+c precedence | (a) overlay sel + cmd+c → overlay text; (b) clear, tmux sel + cmd+c → tmux buffer text; (c) clear all, in Terminal mode, cmd+c → SIGINT (interrupts a `sleep 30`) | All three tiers fire correctly |  |  |

## Pitfall Sweeps

| ID | Pitfall (RESEARCH) | Procedure | Pass Criterion |
|----|---------------------|-----------|----------------|
| PIT-7-1 | Pitfall #1: Modal click leak | Open Help modal (or any modal), click+drag inside the modal area | No tmux selection starts in the underlying pane (forward path correctly gated) |
| PIT-7-6 | Pitfall #6: scroll_generation false-positive on copy-mode highlight repaint | In delegate path, drag-select; while selection is shown, scroll the inner shell with mouse wheel | Selection coords stay anchored; no spurious gen-bump (verify by switching back to overlay path after — overlay selection from a different test render at correct row, not drifted) |

## Phase 6 Regression Sweep

Re-run Phase 6's UAT-6-* cases in mouse-app sessions to confirm no regression. Reference: `.planning/phases/06-text-selection/06-HUMAN-UAT.md`.

| Phase 6 ID | Status |
|------------|--------|
| UAT-6 SEL-01 (drag-select highlight tracks) | PASS / FAIL |
| UAT-6 SEL-02 (cmd+c copy in overlay path) | PASS / FAIL |
| UAT-6 SEL-03 (Esc / click-outside clears) | PASS / FAIL |
| UAT-6 SEL-04 (highlight survives PTY output) | PASS / FAIL |

## Operator Sign-Off

- [ ] All UAT-7-A..K marked PASS (or any FAIL has documented mitigation/follow-up)
- [ ] PIT-7-1 + PIT-7-6 marked PASS
- [ ] Phase 6 regression sweep all PASS
- [ ] Subjective "feels indistinguishable from Ghostty+tmux direct" confirmation: YES / NO

**Operator notes:**

(operator fills in after running)

**Forward-Looking Notes:**

(any items deferred to Phase 8 or future polish — e.g., rectangle-select via Alt-drag if user wants it; tmux 4.x compatibility audit per RESEARCH A1)

## Test Suite Status at UAT Start

```
$ cargo test --all-targets
test result: ok. {N} passed; 0 failed; 0 ignored
```

(operator pastes actual output)
```
  </action>
  <verify>
    <automated>test -f /Users/bayma/dev/lucas/martins/.claude/worktrees/ecstatic-panini-661425/target/release/martins &amp;&amp; test -f /Users/bayma/dev/lucas/martins/.claude/worktrees/ecstatic-panini-661425/.planning/phases/07-tmux-native-main-screen-selection/07-HUMAN-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - `target/release/martins` exists and is executable
    - `.planning/phases/07-tmux-native-main-screen-selection/07-HUMAN-UAT.md` exists with all 11 UAT-7-* rows + 2 PIT-7-* rows + Phase 6 regression sweep + Operator Sign-Off section
    - `cargo test --all-targets` reports the full Phase 7 test suite green at this point (145 tests per Plan 07-05)
  </acceptance_criteria>
  <done>Release build exists; UAT scaffold file created with all 11 cases + pitfall sweeps + Phase 6 regression check + sign-off section.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Operator dual-path UAT — Phase 7 native-feel parity sign-off</name>
  <what-built>
    Phase 7 wires PTY-pane selection in non-mouse-app sessions through the wrapped tmux's native copy-mode (forwarded SGR mouse bytes + tmux 3.6a default copy-pipe-and-cancel + Plan 07-02 ensure_config 3-line override). Mouse-app sessions (vim mouse=a, htop, btop) retain the Phase 6 REVERSED-XOR overlay end-to-end. cmd+c follows a 3-tier precedence (overlay → tmux buffer → SIGINT). Esc follows a 3-tier precedence (overlay clear → tmux PTY forward → fall-through). Tab/workspace switch cancels the outgoing tmux selection.
  </what-built>
  <how-to-verify>
1. Open Ghostty. Split horizontally.
2. Top half: run `tmux new -s baseline`. Inside, run `bash`, `cat /usr/share/dict/words | head -30` so there's text to compare against.
3. Bottom half: run `cd .planning/phases/07-tmux-native-main-screen-selection/.. && /Users/bayma/dev/lucas/martins/.claude/worktrees/ecstatic-panini-661425/target/release/martins`. Open or pick a project; switch to a tab running `bash` (or whatever default agent/shell — confirm it's in non-mouse-app state via `ps`). Run `cat /usr/share/dict/words | head -30` to populate the pane.
4. Open `.planning/phases/07-tmux-native-main-screen-selection/07-HUMAN-UAT.md` in your editor.
5. Walk through each UAT-7-A..K case. For each: run the procedure in BOTH halves of the screen, compare feel, mark Result column with PASS or FAIL, and write Notes if anything diverges from the baseline.
6. Run the PIT-7-1 (modal-click leak) + PIT-7-6 (scroll-during-tmux-selection) sweeps.
7. Run the Phase 6 regression sweep — open `vim`, `:set mouse=a`, run UAT-6 SEL-01..04 from the prior phase's HUMAN-UAT log. Confirm overlay path still works (mouse-app sessions are NOT delegated to tmux per D-01).
8. Fill in the Operator Sign-Off checklist + Forward-Looking Notes.
9. Paste `cargo test --all-targets` output into the Test Suite Status block.
10. Commit the filled UAT log.

**Key visual cues to confirm "native feel":**
- In delegate path (UAT-7-A..F): tmux's own reverse-video highlight appears (a single solid block, not the Phase-6 XOR-style toggle). Drag is fluid, no perceptible lag vs. Ghostty A.
- Auto-copy on mouse-up (UAT-7-A): no cmd+c needed; release the mouse and `pbpaste` immediately returns the selected text. THIS IS THE HEADLINE BEHAVIOR.
- Single-press Esc exits copy-mode (UAT-7-D): if Esc requires two presses, the Plan 07-02 ensure_config override didn't take effect — possible cause: stale `~/.martins/tmux.conf` from a prior run; restart Martins to regenerate.
- In overlay path (UAT-7-G/H): Phase 6 REVERSED-XOR rendering visible (cells inverted in their existing color, not solid block).

**Failure responses:**
- If UAT-7-A FAILS (no auto-copy on release): check `tmux list-keys -T copy-mode-vi MouseDragEnd1Pane` against the running tmux; should be `send-keys -X copy-pipe-and-cancel pbcopy`. If different, the user has a custom global tmux.conf overriding ours — report the conflict.
- If UAT-7-D FAILS (Esc takes two presses): inspect `~/.martins/tmux.conf` — line `bind-key -T copy-mode-vi Escape send-keys -X cancel` must be present. If missing, ensure_config didn't run; rebuild.
- If UAT-7-J FAILS (selection persists across tab switch): trace through Plan 07-03's `set_active_tab` — confirm `crate::tmux::cancel_copy_mode(&name)` is called BEFORE `self.active_tab = index`. Report which session name was used.
- For ANY FAIL: do not edit code — return the populated UAT log + console output and the gsd-plan-phase --gaps flow generates the gap-closure plan.
  </how-to-verify>
  <action>Operator runs the UAT scenarios documented in 07-HUMAN-UAT.md side-by-side against Ghostty+tmux baseline; fills in PASS/FAIL + Notes for each row; signs off when complete. No code changes by Claude during this task — Claude only resumes after operator approves or reports failures.</action>
  <verify>
    <automated>grep -q 'Subjective "feels indistinguishable from Ghostty+tmux direct" confirmation: YES' .planning/phases/07-tmux-native-main-screen-selection/07-HUMAN-UAT.md</automated>
  </verify>
  <done>Operator has filled the UAT log with PASS in all rows AND signed YES on subjective confirmation, OR has filed FAIL details for /gsd-plan-phase 7 --gaps.</done>
  <resume-signal>Type "approved" if all UAT-7-* + PIT-7-* + Phase 6 regression sweep PASS and the headline subjective comparison ("feels indistinguishable from Ghostty+tmux") is YES. Otherwise type "failed: {brief}" with the populated 07-HUMAN-UAT.md attached for /gsd-plan-phase 7 --gaps.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Write PHASE-SUMMARY.md after operator approval</name>
  <read_first>
    - .planning/phases/07-tmux-native-main-screen-selection/07-HUMAN-UAT.md (operator-signed)
    - All Plan 07-01..05 SUMMARY files in the phase directory
    - .planning/phases/06-text-selection/PHASE-SUMMARY.md (precedent format)
  </read_first>
  <action>
After the operator types "approved" in Task 2, write `.planning/phases/07-tmux-native-main-screen-selection/PHASE-SUMMARY.md` aggregating:

1. **Phase Goal recap** (from ROADMAP.md Phase 7 Goal block)
2. **Acceptance criteria status** — SEL-01..SEL-04 confirmed in BOTH paths via UAT-7-* + Phase 6 regression sweep
3. **Plans executed** — 07-01 through 07-05 with one-line each + Plan 07-06 UAT outcome
4. **File modification surface delta** (final tally of LOC added/modified across src/tmux.rs, src/events.rs, src/app.rs, src/pty/session.rs, src/main.rs, src/tmux_native_selection_tests.rs)
5. **Test count delta** — pre-Phase 7 baseline 129 → post-Phase 7 final
6. **Decisions adopted** — list of D-XX from CONTEXT.md and how each was implemented (e.g., "D-09 — researcher narrowed to 3 explicit override lines per RESEARCH §Tmux Defaults; defaults handle MouseDragEnd1Pane / DoubleClick1Pane / TripleClick1Pane")
7. **Deviations from RESEARCH** — none expected; if any landed during execution, list them per Plan 07-XX-SUMMARY.md citations
8. **Forward-Looking Notes** — copied verbatim from operator's UAT sign-off; populates Phase 8 / Roadmap backlog
9. **Operator UAT timestamp + sign-off quote**

Then update `.planning/STATE.md` Phase 7 entry to `Status: completed` with the date, append a Decisions log entry capturing the headline behavior, and update `.planning/REQUIREMENTS.md §Text Selection` SEL-01..04 traceability to add "Validated again in Phase 7 (tmux-native + overlay dual path)".

Finally update `.planning/ROADMAP.md` Phase 7 row in the Progress table to "Complete" with the date.
  </action>
  <verify>
    <automated>test -f /Users/bayma/dev/lucas/martins/.claude/worktrees/ecstatic-panini-661425/.planning/phases/07-tmux-native-main-screen-selection/PHASE-SUMMARY.md &amp;&amp; grep -q 'completed' /Users/bayma/dev/lucas/martins/.claude/worktrees/ecstatic-panini-661425/.planning/STATE.md &amp;&amp; grep -q '7. tmux-native main-screen selection.*Complete' /Users/bayma/dev/lucas/martins/.claude/worktrees/ecstatic-panini-661425/.planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - PHASE-SUMMARY.md exists and references all 6 plans + UAT outcome
    - STATE.md Phase 7 marked completed with date
    - ROADMAP.md Phase 7 row updated to Complete
    - REQUIREMENTS.md §Text Selection has Phase 7 dual-path validation note
  </acceptance_criteria>
  <done>PHASE-SUMMARY.md, STATE.md, ROADMAP.md, REQUIREMENTS.md all updated; phase formally closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator → human-verify checkpoint | Subjective qualitative judgment is the only gate; checklist semi-structures the comparison but the final PASS/FAIL is operator-attested |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-18 | Repudiation | Operator marks PASS without actually checking | accept | Phase 7 is for the single user; same trust model as Phase 6 UAT |
| T-07-19 | Information disclosure | UAT scenarios paste system clipboard contents | accept | Operator-controlled testing on operator-controlled machine; no exfil surface |
</threat_model>

<verification>
After operator approval, all artifacts (07-HUMAN-UAT.md, PHASE-SUMMARY.md) committed to git. STATE.md / ROADMAP.md / REQUIREMENTS.md reflect phase completion. Full test suite still green (145 tests).
</verification>

<success_criteria>
- target/release/martins built clean
- 07-HUMAN-UAT.md scaffolded then operator-filled with UAT-7-A..K + PIT-7-1/6 + Phase 6 regression sweep all PASS
- Operator confirms "feels indistinguishable from Ghostty+tmux direct"
- PHASE-SUMMARY.md written
- STATE.md / ROADMAP.md / REQUIREMENTS.md updated for Phase 7 closure
</success_criteria>

<output>
After completion, the canonical phase artifact is `.planning/phases/07-tmux-native-main-screen-selection/PHASE-SUMMARY.md`. Plan-level summaries (07-01-SUMMARY through 07-06-SUMMARY) link from there.
</output>
